import 'package:flutter/material.dart';

class Notifecations_Screen extends StatelessWidget {
  const Notifecations_Screen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text(
            'Notifecations'
        ),
      ),
      body:Text(
          'Notifecations Screen'
      ),
    );

  }
}
